package utils.enums;

public enum Command {
    INVALID,
    LOGIN,
    REGISTER,
    CREATE_TICKET,
    UPDATE_TICKET,
    GET_AVAILABLE_TICKETS,
    GET_CREATED_TICKETS,
    GET_RESOLVER_TICKETS
}
